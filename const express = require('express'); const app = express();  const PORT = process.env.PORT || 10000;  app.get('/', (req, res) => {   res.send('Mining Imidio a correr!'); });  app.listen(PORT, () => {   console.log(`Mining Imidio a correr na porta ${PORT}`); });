const express = require('express');
const app = express();

const PORT = process.env.PORT || 10000;

app.get('/', (req, res) => {
  res.send('Mining Imidio a correr!');
});

app.listen(PORT, () => {
  console.log(`Mining Imidio a correr na porta ${PORT}`);
});
